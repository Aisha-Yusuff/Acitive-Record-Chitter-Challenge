# The Rakefile uses rake to make helper methods - commands available in the command line.
# The commands allow you to setup, modfiy and change the database structure easily and without touching the db directly
# Rake = a command line task runner, it runs small scripts from prompts in the command line

# require your app file first 
require './app'
require 'sinatra/activerecord/rake'

# You can find a list of command by running 'rake -T'
# The main commands to know: 
# rake db:create 
# Creates the database from DATABASE_URL or config/database.yml for the current RAILS_E… 

# rake db:create_migration 
# Create a migration (parameters: NAME, VERSION) 


# rake db:migrate 
# Migrate the database (options: VERSION=x, VERBOSE=false, SCOPE=blog) 
 
# rake db:rollback # Rolls the schema back to the previous version (specify steps w/ STEP=n) 

# rake db:seed 
# Loads the seed data from db/seeds.rb 
